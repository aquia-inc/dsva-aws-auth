[package]
name = "aws-auth"
version = "0.1.0"
edition = "2021"
description = "Simple CLI tool for AWS MFA session authentication"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
